# MegaChat — Мессенджер нового поколения

Полнофункциональный мессенджер с реальной базой данных SQLite, защитой от DDoS и расширенной админ-панелью.

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
# Frontend
npm install

# Backend
cd server
npm install
cd ..
```

### 2. Соберите frontend

```bash
npm run build
```

### 3. Запустите сервер

```bash
cd server
npm start
```

Сервер запустится на `http://localhost:3001`

### 4. Откройте в браузере

```
http://localhost:3001
```

Первый зарегистрированный пользователь автоматически получает роль **superadmin**.

---

## 📋 Использование

### 1. Основные функции
- **Лента** — создание постов с хэштегами и изображениями
- **Чаты** — личные сообщения и групповые чаты
- **Видео** — загрузка и просмотр видео
- **Shorts** — короткие видео в вертикальной ленте
- **Музыка** — загрузка и прослушивание треков
- **Сторис** — временные истории (24 часа)
- **Профиль** — настройка аватара, обложки, био, статуса

### 2. Админ-панель (для admin/superadmin)
- **Дашборд** — статистика платформы
- **Пользователи** — управление пользователями
- **Все аккаунты** — полное редактирование всех аккаунтов через файл `server/data/accounts.json`
- **Жалобы** — модерация контента
- **IP баны** — блокировка IP адресов
- **Сессии** — управление активными сессиями
- **Видеозвонки** — отправка видеозвонков пользователям
- **Логи** — просмотр действий администраторов

### 3. Управление аккаунтами через файл

Все аккаунты сохраняются в файле `server/data/accounts.json`. Вы можете:

#### Через админ-панель:
1. Перейдите в раздел **"Все аккаунты"**
2. Нажмите **"Создать аккаунт"** для добавления нового пользователя
3. Нажмите на иконку карандаша для редактирования существующего аккаунта
4. Можно изменить: имя, email, пароль, роль, аватар, обложку, био, статус, бан

#### Прямое редактирование файла:
```bash
# Откройте файл accounts.json
nano server/data/accounts.json
# или
code server/data/accounts.json
```

Структура файла:
```json
{
  "accounts": [
    {
      "id": "uuid...",
      "username": "admin",
      "email": "admin@example.com",
      "password_hash": "$2a$12$...",
      "role": "superadmin",
      "avatar": "https://...",
      "cover": "https://...",
      "bio": "О себе",
      "status": "🟢 Онлайн",
      "is_banned": false,
      "ban_reason": "",
      "created_at": 1234567890
    }
  ],
  "lastUpdated": "2024-01-01T00:00:00.000Z"
}
```

После редактирования файла изменения автоматически синхронизируются с базой данных при следующем запросе.

---

## 🔐 Безопасность

### Защита от DDoS
- ✅ **Rate Limiting**: 100 запросов за 5 минут с одного IP
- ✅ **IP баны**: возможность блокировать IP адреса через админ-панель
- ✅ **Helmet**: защита HTTP заголовков
- ✅ **CORS**: настроен для безопасности
- ✅ **JWT токены**: без хранения в localStorage (только в памяти)

### Защита данных
- ✅ **bcrypt(12)**: хэширование паролей
- ✅ **Prepared statements**: защита от SQL-инъекций
- ✅ **Валидация данных**: проверка всех входных данных
- ✅ **Audit Log**: все действия админов записываются

### Сессии
- ✅ **Активные сессии**: просмотр и управление в админ-панели
- ✅ **Автоматическое обновление**: last_active обновляется при каждом запросе
- ✅ **Принудительный выход**: админ может завершить любую сессию

---

## 📱 Функции

### Мессенджер
- ✅ Создание чатов (личные, группы, каналы)
- ✅ Отправка сообщений
- ✅ Реакции на сообщения
- ✅ Статусы прочитано/доставлено

### Лента
- ✅ Создание постов с хэштегами
- ✅ Загрузка изображений по URL
- ✅ Лайки
- ✅ Комментарии

### Видео
- ✅ Загрузка видео с превью
- ✅ Теги и описание
- ✅ Счётчик просмотров

### Shorts
- ✅ Вертикальная лента коротких видео
- ✅ Навигация свайпом

### Музыка
- ✅ Загрузка треков с обложками
- ✅ Плеер с контролами

### Сторис
- ✅ Создание историй (24 часа)
- ✅ Просмотр

### Профиль
- ✅ Кастомизация (аватар, обложка, био, статус)
- ✅ Все данные хранятся в БД (не в localStorage)
- ✅ Статистика (подписчики, подписки, посты)

---

## 🛡 Админ-панель

### Дашборд
- ✅ Статистика пользователей, постов, видео, сообщений
- ✅ Онлайн пользователи
- ✅ Жалобы в очереди

### Управление пользователями
- ✅ Просмотр всех пользователей
- ✅ Бан/разбан пользователей
- ✅ Удаление пользователей (только superadmin)
- ✅ Смена ролей

### Жалобы
- ✅ Просмотр всех жалоб
- ✅ Отклонить/решить жалобы

### IP баны ⭐ НОВОЕ
- ✅ Блокировка IP адресов
- ✅ Временные баны (указание часов)
- ✅ Постоянные баны
- ✅ Просмотр всех банов
- ✅ Разблокировка IP

### Активные сессии ⭐ НОВОЕ
- ✅ Просмотр всех активных сессий
- ✅ IP адрес и устройство пользователя
- ✅ Последняя активность
- ✅ Принудительное завершение сессий

### Видеозвонки ⭐ НОВОЕ
- ✅ Отправка видеозвонков пользователям
- ✅ Если пользователь онлайн — звонок приходит сразу
- ✅ Если оффлайн — звонок доставляется при входе
- ✅ Можно указать URL видео или отправить только аудио
- ✅ Все действия записываются в Audit Log

### Audit Log
- ✅ Все действия админов
- ✅ IP адрес и время действия
- ✅ Детали каждого действия

---

## 🌐 Хостинг

### LocalTunnel (проще всего)

В **новой консоли** (не закрывая сервер):

```bash
npx localtunnel --port 3001
```

Получите публичную ссылку типа `https://xxx-xxx-xxx.loca.lt`

### Pinggy

```bash
ssh -p 443 -R0:localhost:3001 pinggy@a.pinggy.io
```

---

## 🗄️ База данных

**Тип:** SQLite (sql.js — WebAssembly)  
**Файл:** `server/data/messenger.db`  
**Режим:** WAL для производительности

### Таблицы:
- `users` — пользователи (bcrypt пароли)
- `chats` / `chat_members` — чаты и участники
- `messages` / `reactions` — сообщения и реакции
- `posts` / `likes` / `comments` — посты и лента
- `stories` — истории (24 часа)
- `videos` — видео
- `music` — музыка
- `subscriptions` / `friends` — подписки
- `notifications` — уведомления
- `reports` — жалобы
- `audit_log` — лог действий админов
- `ip_bans` — баны IP адресов
- `active_sessions` — активные сессии
- `system_settings` — системные настройки
- `login_attempts` — попытки входа

---

## 📡 API Endpoints

### Auth
- `POST /api/auth/register` — регистрация
- `POST /api/auth/login` — вход
- `POST /api/auth/logout` — выход
- `GET /api/auth/me` — текущий пользователь

### Users
- `GET /api/users` — список пользователей
- `GET /api/users/:id` — профиль
- `PUT /api/users/me` — обновить профиль
- `POST /api/users/:id/follow` — подписаться
- `DELETE /api/users/:id/follow` — отписаться

### Chats & Messages
- `GET /api/chats` — мои чаты
- `POST /api/chats` — создать чат
- `GET /api/chats/:id/messages` — сообщения чата
- `POST /api/chats/:id/messages` — отправить сообщение
- `POST /api/messages/:id/react` — реакция

### Posts
- `GET /api/posts` — лента
- `POST /api/posts` — создать пост
- `POST /api/posts/:id/like` — лайк/анлайк
- `DELETE /api/posts/:id` — удалить

### Stories / Videos / Music
- `GET /api/stories` — активные сторис
- `POST /api/stories` — создать сторис
- `GET /api/videos` — видео
- `POST /api/videos` — загрузить видео
- `GET /api/music` — треки
- `POST /api/music` — загрузить трек

### Admin (требует роль admin/superadmin)
- `GET /api/admin/stats` — статистика
- `GET /api/admin/users` — все пользователи
- `PUT /api/admin/users/:id/ban` — заблокировать
- `PUT /api/admin/users/:id/unban` — разблокировать
- `PUT /api/admin/users/:id/role` — сменить роль
- `DELETE /api/admin/users/:id` — удалить (только superadmin)
- `GET /api/admin/reports` — жалобы
- `PUT /api/admin/reports/:id` — обработать жалобу
- `GET /api/admin/audit-log` — лог действий
- `GET /api/admin/ip-bans` — IP баны
- `POST /api/admin/ip-bans` — заблокировать IP
- `DELETE /api/admin/ip-bans/:id` — разблокировать IP
- `GET /api/admin/sessions` — активные сессии
- `DELETE /api/admin/sessions/:id` — завершить сессию
- `POST /api/admin/call` — видеозвонок пользователю
- `GET /api/admin/settings` — настройки
- `PUT /api/admin/settings` — обновить настройки

---

## 🛠 Роли

| Роль | Права |
|------|-------|
| `user` | Обычный пользователь |
| `moderator` | + модерация жалоб, бан |
| `admin` | + управление пользователями, IP баны, сессии, видеозвонки |
| `superadmin` | + удаление пользователей, смена ролей, системные настройки |

**Первый зарегистрированный = superadmin**

---

## 📂 Структура проекта

```
megachat/
├── server/              # Backend (Node.js + Express + SQLite)
│   ├── server.js        # Главный сервер + API + защита
│   ├── db.js            # Инициализация SQLite + миграции
│   ├── package.json     # Зависимости сервера
│   └── data/            # Автоматически создаётся
│       └── messenger.db # SQLite база данных
├── src/                 # Frontend (React + TypeScript)
│   ├── App.tsx          # Главный компонент
│   ├── store.ts         # API клиент (без localStorage)
│   ├── types.ts         # TypeScript типы
│   └── index.css        # Стили (Tailwind)
├── dist/                # Собранный фронтенд (после npm run build)
├── package.json         # Зависимости фронтенда
├── vite.config.js       # Конфигурация Vite
├── tsconfig.json        # Конфигурация TypeScript
└── README.md            # Этот файл
```

---

## ❓ Проблемы и решения

### "Cannot find module"
```bash
npm install
cd server && npm install
```

### "Port already in use"
Измените порт в `server/server.js`:
```javascript
const PORT = process.env.PORT || 3002; // измени 3001 на 3002
```

### "Database error"
Удалите `server/data/messenger.db` и перезапустите сервер — БД создастся заново

### "Cannot connect to server"
Убедитесь что сервер запущен:
```bash
cd server && npm start
```

### "Rate limit exceeded"
Подождите 5 минут или используйте другой IP

---

## 🔧 Технологии

**Backend:**
- Node.js + Express
- SQLite (sql.js — WebAssembly, работает без компиляции C++)
- Socket.IO (WebSocket)
- JWT + bcrypt
- Helmet + CORS
- Rate limiting

**Frontend:**
- React 18
- TypeScript
- Vite
- Tailwind CSS 4
- Lucide Icons

---

## 📝 Команды

```bash
# Разработка
npm run dev              # Frontend (port 5173)
cd server && npm start   # Backend (port 3001)

# Сборка
npm run build            # Собрать frontend в dist/

# Продакшн
cd server && npm start   # Запустит и API и раздаст dist/
```

---

## 🎯 Итого

✅ Полностью рабочий мессенджер  
✅ Реальная SQLite база данных на твоём ПК  
✅ Без фейковых данных — всё настоящее  
✅ Все функции работают через API  
✅ Данные хранятся в БД (не в localStorage)  
✅ Защита от DDoS (rate limiting, IP баны)  
✅ Расширенная админ-панель  
✅ Видеозвонки пользователям  
✅ Управление сессиями  
✅ Audit log для всех действий  
✅ Безопасность (bcrypt, JWT, Helmet)  
✅ Готов к хостингу  

**Запускай и пользуйся!** 🚀

---

© MegaChat 2024 — Мессенджер нового поколения
